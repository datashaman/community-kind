<!DOCTYPE html><html lang="{{ str_replace('_', '-', app()->getLocale()) }}"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>{{ $opportunity->title }} · {{ $organisation->name }}</title><style>:root{color-scheme:light dark;font-family:ui-sans-serif,system-ui,sans-serif;line-height:1.6}main{width:min(42rem,calc(100% - 2rem));margin:3rem auto}form{display:grid;gap:1rem}label,fieldset{display:grid;gap:.35rem}input,button{font:inherit;padding:.7rem}.errors{color:#b91c1c}</style></head><body><main><p><a href="{{ route('public.volunteers.index', $organisation->slug) }}">← Opportunities</a></p><h1>{{ $opportunity->title }}</h1><p>{{ $opportunity->summary }}</p>@if($errors->any())<div class="errors" role="alert">Please correct the highlighted registration details.</div>@endif<form method="post" action="{{ route('public.volunteers.store', [$organisation->slug, $opportunity]) }}">@csrf<label>Name<input name="name" value="{{ old('name') }}" required autocomplete="name">@error('name')<span>{{ $message }}</span>@enderror</label><label>Email<input name="email" type="email" value="{{ old('email') }}" required autocomplete="email">@error('email')<span>{{ $message }}</span>@enderror</label><fieldset><legend>Interests</legend>@foreach($opportunity->interest_tags as $interest)<label><input type="checkbox" name="interests[]" value="{{ $interest }}"> {{ $interest }}</label>@endforeach</fieldset><fieldset><legend>Availability</legend>@foreach(['weekday_morning'=>'Weekday mornings','weekday_afternoon'=>'Weekday afternoons','weekday_evening'=>'Weekday evenings','weekend'=>'Weekends'] as $value=>$label)<label><input type="checkbox" name="availability[]" value="{{ $value }}"> {{ $label }}</label>@endforeach</fieldset><input type="hidden" name="consent_email" value="0"><label><input type="checkbox" name="consent_email" value="1"> Email me volunteer and supporter updates</label><button type="submit" @disabled(!$opportunity->acceptsRegistrations())>Register to volunteer</button></form></main></body></html>
